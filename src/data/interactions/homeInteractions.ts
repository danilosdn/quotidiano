import { createHouseInteractions } from '../../game/house/HouseObjectRegistry';

/** Data-driven interactions derived from the House V3 object registry. */
export const HOME_INTERACTIONS = createHouseInteractions();
